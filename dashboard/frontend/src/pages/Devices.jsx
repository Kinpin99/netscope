import { useMemo,useState } from 'react';import { useNavigate } from 'react-router-dom';import { Search,ChevronRight } from 'lucide-react';import { useDevices } from '../context/DeviceContext';import { friendlyStatus,deviceTypeLabel } from '../utils/friendly';import HealthBar from '../components/ui/HealthBar';import EmptyPanel from '../components/ui/EmptyPanel'
export default function Devices(){const nav=useNavigate();const {devices,loading}=useDevices();const [search,setSearch]=useState('');const [status,setStatus]=useState('all');const [sort,setSort]=useState('name');const filtered=useMemo(()=>{let x=devices.filter(d=>{const q=search.toLowerCase();return (!q||`${d.name} ${d.ip} ${d.building}`.toLowerCase().includes(q))&&(status==='all'||d.status===status)});return x.sort((a,b)=>sort==='health'?(Number(b.health_score)||-1)-(Number(a.health_score)||-1):String(a.name).localeCompare(String(b.name)))},[devices,search,status,sort]);return <div className="space-y-4"><div className="flex gap-3 flex-wrap"><div className="relative flex-1 min-w-[240px] max-w-[420px]"><Search size={14} className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--muted)]"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search by name, address or building" className="w-full pl-9 pr-3 py-2 bg-[var(--surface)] border border-[var(--border)] rounded-[7px] text-[11px] outline-none"/></div><select value={status} onChange={e=>setStatus(e.target.value)} className="px-3 py-2 bg-[var(--surface)] border border-[var(--border)] rounded-[7px] text-[11px]"><option value="all">All states</option><option value="healthy">Good</option><option value="degraded">Needs attention</option><option value="critical">Urgent</option><option value="unknown">No recent data</option></select><select value={sort} onChange={e=>setSort(e.target.value)} className="px-3 py-2 bg-[var(--surface)] border border-[var(--border)] rounded-[7px] text-[11px]"><option value="name">Sort by name</option><option value="health">Sort by health</option></select><span className="ml-auto text-[10px] text-[var(--muted)] self-center">{filtered.length} devices</span></div><div className="bg-[var(--surface)] border border-[var(--border)] rounded-[7px] overflow-hidden">{loading?<div className="p-4"><div className="skeleton h-52"/></div>:!filtered.length?<EmptyPanel message="No devices match this view."/>:<div className="overflow-x-auto"><table className="w-full text-[11px]"><thead><tr className="bg-[var(--surface2)] text-[9px] uppercase tracking-[.06em] text-[var(--muted)]"><th className="text-left px-4 py-3">Device</th><th className="text-left px-3">Address</th><th className="text-left px-3">Location</th><th className="text-left px-3">Type</th><th className="text-left px-3">State</th><th className="text-left px-3">Health</th><th/></tr></thead><tbody>{filtered.map(d=><tr key={d.ip} onClick={()=>nav(`/devices/${d.ip}`)} className="border-t border-[var(--border)] hover:bg-white/[.018] cursor-pointer"><td className="px-4 py-3 font-semibold">{d.name}</td><td className="px-3 font-mono text-[var(--muted)]">{d.ip}</td><td className="px-3 text-[var(--muted)]">{d.building||'Unassigned'}{d.floor?` · Floor ${d.floor}`:''}</td><td className="px-3 text-[var(--muted)]">{deviceTypeLabel(d.device_type)}</td><td className="px-3"><span style={{color:d.status==='healthy'?'var(--ok)':d.status==='critical'?'var(--crit)':d.status==='degraded'?'var(--warn)':'var(--muted)'}}>{friendlyStatus(d.status)}</span></td><td className="px-3">{d.health_score==null?<span className="text-[var(--muted)]">—</span>:<HealthBar score={Math.round(d.health_score)} width={54}/>}</td><td className="pr-4"><ChevronRight size={14} className="text-[var(--muted)]"/></td></tr>)}</tbody></table></div>}</div></div>}
