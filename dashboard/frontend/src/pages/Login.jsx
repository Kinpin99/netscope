import { useState } from 'react';import { useAuth } from '../context/AuthContext';import { ShieldCheck } from 'lucide-react'
export default function Login(){const {login,loading,error}=useAuth();const [username,setUsername]=useState('');const [password,setPassword]=useState('');const submit=async e=>{e.preventDefault();await login(username,password)};return <div className="min-h-screen bg-[var(--bg)] grid place-items-center p-6"><div className="w-full max-w-[380px] bg-[var(--surface)] border border-[var(--border)] rounded-xl p-7"><div className="flex items-center gap-3 mb-7"><div className="w-10 h-10 rounded-lg bg-[var(--accent)] grid place-items-center text-white font-extrabold text-lg">N</div><div><h1 className="text-lg font-bold">NetScope</h1><p className="text-[11px] text-[var(--muted)]">School network monitoring</p></div></div><div className="flex gap-2 items-start p-3 rounded-lg bg-[var(--surface2)] mb-5"><ShieldCheck size={17} className="text-[var(--ok)] mt-0.5"/><p className="text-[11px] text-[var(--muted)] leading-5">Sign in with the account created when the system was set up.</p></div>{error&&<div className="login-banner login-banner--error mb-4">{error}</div>}<form onSubmit={submit} className="space-y-4"><label className="block text-[11px] text-[var(--muted)]">Username<input className="mt-2 w-full px-3 py-2.5 bg-[var(--bg)] border border-[var(--border)] rounded-lg text-[12px] outline-none focus:border-[var(--accent)]" value={username} onChange={e=>setUsername(e.target.value)} required/></label><label className="block text-[11px] text-[var(--muted)]">Password<input type="password" className="mt-2 w-full px-3 py-2.5 bg-[var(--bg)] border border-[var(--border)] rounded-lg text-[12px] outline-none focus:border-[var(--accent)]" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button disabled={loading} className="w-full py-2.5 rounded-lg bg-[var(--accent)] text-white text-[12px] font-semibold disabled:opacity-60">{loading?'Signing in…':'Sign in'}</button></form></div></div>}
