import { useMemo,useState } from 'react';import { Search } from 'lucide-react';import { useAlerts } from '../context/AlertContext';import FriendlyAlertItem from '../components/ui/FriendlyAlertItem';import EmptyPanel from '../components/ui/EmptyPanel';
export default function Alerts(){const {openAlerts}=useAlerts();const [expanded,setExpanded]=useState(null);const [severity,setSeverity]=useState('all');const [type,setType]=useState('all');const [search,setSearch]=useState('');const list=useMemo(()=>openAlerts.filter(a=>(severity==='all'||String(a.severity)===severity)&&(type==='all'||String(a.detector||a.anomaly_type)===type)&&(!search||`${a.device_id} ${a.device_ip}`.toLowerCase().includes(search.toLowerCase()))),[openAlerts,severity,type,search]);return <div className="space-y-4"><div className="flex gap-3 flex-wrap"><select value={severity} onChange={e=>setSeverity(e.target.value)} className="px-3 py-2 bg-[var(--surface)] border border-[var(--border)] rounded-[7px] text-[11px]"><option value="all">All importance levels</option><option value="critical">Urgent</option><option value="high">High</option><option value="medium">Attention</option><option value="info">Notice</option></select><select value={type} onChange={e=>setType(e.target.value)} className="px-3 py-2 bg-[var(--surface)] border border-[var(--border)] rounded-[7px] text-[11px]"><option value="all">All issue types</option><option value="bandwidth">Heavy traffic</option><option value="portscan">Port scan</option><option value="device_behavior">Unusual device activity</option><option value="protocol">Unexpected network use</option></select><div className="relative"><Search size={14} className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--muted)]"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Filter by device" className="pl-9 pr-3 py-2 bg-[var(--surface)] border border-[var(--border)] rounded-[7px] text-[11px] outline-none"/></div><span className="ml-auto text-[10px] text-[var(--muted)] self-center">{list.length} open alerts</span></div><div className="space-y-2">{list.map(a=><FriendlyAlertItem key={a.event_id} alert={a} expanded={expanded===a.event_id} onToggle={()=>setExpanded(expanded===a.event_id?null:a.event_id)}/>)}{!list.length&&<div className="panel"><EmptyPanel message="There are no open alerts in this view."/></div>}</div></div>}
